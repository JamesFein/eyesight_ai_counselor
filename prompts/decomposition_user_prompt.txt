请将以下用户问题使用MECE思维分解为子检索肯定句：

<user_question>
{{original_query}}
</user_question>

请严格按照system prompt中的要求：
1. 使用MECE思维将问题拆分为1-10个相互独立、完全穷尽的子检索
2. 每个子检索必须只关注一个单一的维度或信息点
3. **每个statement必须是语法完整的陈述句（主语+谓语+宾语），禁止使用名词短语**
4. 输出标准JSON格式，包含sub_queries数组和reasoning说明

立即开始分解并输出JSON结果。
你的最终回复必须是**单一 JSON 对象**（Object），且必须能被 `json.loads()` 直接解析。
